//  prescription status enum

// import { CANCELLED } from 'dns';

export enum prescriptionStatus {
  ACTIVE = 'active',
  INACTIVE = 'inactive',
  CANCELLED = 'cancelled',
  COMPLETED = 'completed',
}
